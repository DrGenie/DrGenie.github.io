---
title: "Attention, attribute ordering, and what people actually read"
description: "Bringing eye-tracking evidence into preference measurement: what moves when a choice task is reordered, and what that means for the estimates."
---

Choice modelling assumes the respondent read the alternatives. Every coefficient
in a discrete choice experiment is an answer to the question *how much does this
attribute matter*, and that answer only means what we say it means if the person
looked at the attribute.

They frequently did not. Eye tracking shows attention across a choice task is
uneven and patterned: the top of the table is read, the bottom is sampled, and
some attributes are skipped almost entirely by some respondents and treated as
decisive by others. The pattern is stable enough to model and consequential
enough to change estimates.

## Order is not neutral

The clearest demonstration is reordering. Take the same attributes, the same
levels, the same design, and move one attribute from the second row to the
seventh. The estimated coefficient changes. Nothing about the underlying
preference has changed, so either the elicitation or the interpretation is wrong.

```{=html}
<figure>
<svg viewBox="0 0 700 300" xmlns="http://www.w3.org/2000/svg" role="img" aria-labelledby="fig-title fig-desc">
  <title id="fig-title">Schematic of fixation patterns under two attribute orders</title>
  <desc id="fig-desc">Two identical choice tables with the attribute rows in different orders. In each, circles mark fixations and their size marks dwell time. Attention concentrates in the upper rows in both cases, so the attribute placed near the top receives more attention than the same attribute placed near the bottom.</desc>

  <g font-family="Spectral, Georgia, serif" font-size="12" fill="#5b6d76">

    <!-- Panel A -->
    <text x="0" y="14" fill="#0d171d" font-size="13" font-weight="600">Order 1</text>
    <g transform="translate(0,28)">
      <rect x="0" y="0" width="300" height="26" fill="#eef2f3"/>
      <line x1="0" y1="26" x2="300" y2="26" stroke="#16232b" stroke-width="1"/>
      <text x="10" y="18">Option A</text><text x="150" y="18">Option B</text>
      <g stroke="#e7ecee" stroke-width="1">
        <line x1="0" y1="60" x2="300" y2="60"/><line x1="0" y1="94" x2="300" y2="94"/>
        <line x1="0" y1="128" x2="300" y2="128"/><line x1="0" y1="162" x2="300" y2="162"/>
        <line x1="0" y1="196" x2="300" y2="196"/>
      </g>
      <text x="10" y="50">Out-of-pocket cost</text>
      <text x="10" y="84">Waiting time</text>
      <text x="10" y="118">Continuity of care</text>
      <text x="10" y="152">Consultation length</text>
      <text x="10" y="186">Travel required</text>

      <!-- saccade path -->
      <polyline points="120,42 232,42 128,76 236,76 132,110 226,144 138,178"
                fill="none" stroke="#7c2233" stroke-width="0.8" opacity="0.45"/>
      <!-- fixations, radius encodes dwell -->
      <g fill="#7c2233" opacity="0.55">
        <circle cx="120" cy="42" r="13"/><circle cx="232" cy="42" r="12"/>
        <circle cx="128" cy="76" r="11"/><circle cx="236" cy="76" r="10"/>
        <circle cx="132" cy="110" r="7"/>
        <circle cx="226" cy="144" r="4.5"/>
        <circle cx="138" cy="178" r="3"/>
      </g>
    </g>

    <!-- Panel B -->
    <g transform="translate(390,0)">
      <text x="0" y="14" fill="#0d171d" font-size="13" font-weight="600">Order 2</text>
      <g transform="translate(0,28)">
        <rect x="0" y="0" width="300" height="26" fill="#eef2f3"/>
        <line x1="0" y1="26" x2="300" y2="26" stroke="#16232b" stroke-width="1"/>
        <text x="10" y="18">Option A</text><text x="150" y="18">Option B</text>
        <g stroke="#e7ecee" stroke-width="1">
          <line x1="0" y1="60" x2="300" y2="60"/><line x1="0" y1="94" x2="300" y2="94"/>
          <line x1="0" y1="128" x2="300" y2="128"/><line x1="0" y1="162" x2="300" y2="162"/>
          <line x1="0" y1="196" x2="300" y2="196"/>
        </g>
        <text x="10" y="50">Travel required</text>
        <text x="10" y="84">Consultation length</text>
        <text x="10" y="118">Continuity of care</text>
        <text x="10" y="152">Waiting time</text>
        <text x="10" y="186">Out-of-pocket cost</text>

        <polyline points="120,42 232,42 128,76 236,76 132,110 226,144 138,178"
                  fill="none" stroke="#7c2233" stroke-width="0.8" opacity="0.45"/>
        <g fill="#7c2233" opacity="0.55">
          <circle cx="120" cy="42" r="13"/><circle cx="232" cy="42" r="12"/>
          <circle cx="128" cy="76" r="11"/><circle cx="236" cy="76" r="10"/>
          <circle cx="132" cy="110" r="7"/>
          <circle cx="226" cy="144" r="4.5"/>
          <circle cx="138" cy="178" r="3"/>
        </g>
      </g>
    </g>
  </g>
</svg>
<figcaption>Schematic, not data. Circles are fixations and their size is dwell time. The attention pattern is held constant across the two panels and only the row order changes, which is the point: cost is read closely on the left and barely reached on the right, though the respondent's preference for cost has not changed.</figcaption>
</figure>
```

## What the process evidence buys

**A check on the design.** If a whole attribute goes unattended by most
respondents, its coefficient is not measuring a preference. Knowing that before
publication is better than after.

**A model of how, not just what.** Attention data supports models where the
decision rule itself varies: some people compare alternatives attribute by
attribute, some evaluate each alternative whole, and some eliminate on one
attribute and never look further. These produce different choices from identical
preferences.

**A reason to standardise reporting.** If order effects are real, then attribute
order belongs in the methods section alongside the design, and reordering across
respondents belongs in the design itself.

## Software

The analysis pipeline for the attribute-ordering work, including the sequence
analysis of fixation data, is on [GitHub](https://github.com/DrGenie).

## Related

- [Health preferences and behavioural economics](/research/projects/health-preferences-dce/)
- [Papers](/publications/)
